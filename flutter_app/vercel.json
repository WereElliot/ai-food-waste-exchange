{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/web"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "installCommand": "if [ -d \"flutter\" ]; then cd flutter && git pull && cd .. ; else git clone https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter doctor && ./flutter/bin/flutter clean && ./flutter/bin/flutter config --enable-web",
  "buildCommand": "./flutter/bin/flutter build web --release"
}
